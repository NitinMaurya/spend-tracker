[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    golden: characterization test against a real statement PDF (needs sample_statements/)
    red: drives unimplemented behaviour (expected to fail until built)
